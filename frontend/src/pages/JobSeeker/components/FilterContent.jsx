import React from "react";

const FilterContent = ({
  toggleSection,
  clearAllfilters,
  expandedSections,
  filters,
  handleFilterChange,
}) => {
  return <div>FilterContent</div>;
};

export default FilterContent;
